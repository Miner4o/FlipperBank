# Tasks - Flipper Bank Terminal Frontend (Iteration 6)

- [x] Update `task.md` with new goals
- [x] Modify `index.html` to add the transparent `#win-hotspot` element preceding `#win-controls`
- [x] Modify `styles.css` to absolute-position the window controls at the top-right edge and hide them by default (revealed on hover)
- [x] Adjust hotspot and controls with negative offsets to snap flush with the window border
- [x] Adjust button height to 44px to prevent icon clipping
- [x] Start the application and test window buttons hover and visibility actions
- [x] Update `walkthrough.md` with verification details
